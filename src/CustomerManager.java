import java.util.List;

public interface CustomerManager {

    /**
     * Adds a new Customer object to the system.
     *
     * @param customer The Customer object representing the customer to be added.
     */
    void addCustomer(Customer customer);

}
